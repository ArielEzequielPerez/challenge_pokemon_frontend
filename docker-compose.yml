services:
  frontend:
    build: .
    container_name: pokemon-dev
    ports:
      - '5174:5000'
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - CHOKIDAR_USEPOLLING=true
